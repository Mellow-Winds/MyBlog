# MyBlog Alpha Plan

> 日期：2026-09-12  
> 阶段：Alpha 规划  
> 目标：构建一个可由 GitHub Pages 直接托管的个人主页静态网站。  
> 规划起点：先完成仓库初始化、设计调研和计划；后续实现按用户确认的范围逐步推进。

## 1. 已确认需求

### 1.1 产品目标

构建一个个人主页静态网站，最终可以直接放入 GitHub 仓库并由 GitHub Pages 托管。网站不依赖后端、数据库或必须运行的服务器；访客只需要浏览器即可查看内容。

### 1.2 视觉目标

网站采用左右双栏布局，视觉设计语言完全参考工作区内的 `MD_Demo`。这里的“参考”指颜色、排版、空间、圆角、表面层级、交互状态和响应式方式保持同一设计系统；信息架构仍以本项目的个人主页需求为准，不复制 `MD_Demo` 的设计展厅页面。

### 1.3 首期板块

1. 关于我
2. 学在南雍
3. 玩在金陵
4. 南雍杂忆
5. 文章检索
6. GitHub

所有功能性图标和装饰性图标均使用 SVG。不得使用 emoji、图标字体或风格不一致的外部图标集替代 SVG；纯图标按钮必须有可访问名称。

## 2. 本轮已完成事项

- 已在 `MyBlog` 根目录初始化 Git 仓库。
- 默认分支已设为 `main`，适合后续接入 GitHub Pages。
- 已创建 `.gitignore`。
- 已创建 `devlog/` 文件夹和本文件。
- 已按项目启动约定创建 `AGENTS.md`，并将其加入 `.gitignore`；它是本地协作说明，不作为仓库交付内容提交。
- 已核读 `MD_Demo` 的 `AGENTS.md`、`README.md`、`rules`、`develop/design.md`、当前入口、主要 CSS/JS、最近开发日志和验收测试。
- 已配置 GitHub remote，并保持本地 `main` 与 `origin/main` 同步；GitHub Pages 发布设置由用户在仓库后台确认。

### 2.1 Alpha 实施 01：液态玻璃双栏骨架（2026-09-12）

- 已创建根目录静态入口 `index.html` 和 `assets/css/style.css`，没有引入框架、后端或外部资源。
- 桌面端采用左侧导航栏 + 右侧空白主内容区域；液态玻璃只用于左侧导航表面，右侧保留页面背景，不再渲染大卡片容器。
- 左侧已加入“关于我、学在南雍、玩在金陵、南雍杂忆、文章检索、GitHub”六项并列导航，以及品牌标识。
- 所有当前图标均为内联 SVG；当前右侧已加入导航占位内容，尚未加入个人资料、文章数据或真实内容。
- 正常显示模式使用半透明表面、背景模糊、背景光斑和高光边缘；增强对比度、强制配色或浏览器不支持模糊时自动回退为实色，确保文字和边界可读。
- 已用 Chrome 无头截图检查桌面和 390px 移动端结构；当前截图环境触发了无障碍回退，因此截图呈现不透明版本，属于预期行为。

### 2.2 Alpha 实施 01 修正：强化液态玻璃层次（2026-09-12）

- 根据视觉复核，撤回“浅色圆角卡片”的观感，改用更接近 `MD_Demo` 当前液态玻璃预览的材质参数。
- 背景增加由基础蓝和容器蓝形成的柔和光斑与渐变，确保导航玻璃层确实有可透过、可模糊的背景内容。
- 侧栏使用更厚的半透明表面和更强模糊，右侧不设置大面积表面，让导航成为页面当前阶段的唯一玻璃容器。
- 面板边缘改为白色高光边缘，活动导航改为半透明白色浮层，降低厚重描边感。
- 仍保留 `prefers-contrast`、`forced-colors` 和不支持 `backdrop-filter` 时的可读性回退；Windows“透明效果”不关闭液态玻璃。

### 2.3 Alpha 实施 01 兼容修正：对齐 MD_Demo 材质状态（2026-09-12）

- 页面入口显式声明 `data-material-mode="liquid-glass"`，即使初始化脚本未执行，CSS 也会按液态玻璃状态渲染。
- `assets/js/app.js` 负责绘制稳定的底层渐变，并监听减少透明度、高对比度和强制配色偏好；这些偏好开启时移除内联渐变，避免覆盖无障碍回退。
- 为支持直接用 Edge 打开本地 `file:///` 页面，初始化脚本使用无模块普通脚本；它不依赖框架、构建工具或网络资源。
- 在模拟普通透明度的 Edge 环境中确认面板实际计算为 `blur(24px) saturate(135%)`；已检查 1440×900、1024×768、768×1024、390×844、320×800 均无横向溢出。

### 2.4 Windows 透明效果独立显示（2026-09-12）

- 用户明确要求关闭 Windows“透明效果”后仍显示液态玻璃；本要求取代此前关于减少透明度自动回退的约定。
- CSS 和 JS 均不再以 `prefers-reduced-transparency` 作为关闭材质的条件；高对比度、强制配色和不支持模糊的回退继续保留。
- 入口资源增加版本号，避免部署后继续使用旧 CSS / JS 缓存。
- Edge 实际页面在模拟 `prefers-reduced-transparency: reduce` 与 `no-preference` 下的模糊、背景透明度、阴影保持一致；初次加载与偏好切换均通过，390px 页面无横向溢出，高对比度和强制配色回退通过。

### 2.5 Alpha 实施 02：导航交互与右侧留白（2026-09-12）

- 移除右侧 `main` 的 `glass-surface`，保留双栏几何区域和响应式留白，避免空内容被误读为一张大卡片。
- 导航点击后立即更新活动态和 `aria-current="page"`；刷新或直接访问带 hash 的地址时，会根据 hash 恢复当前项。
- GitHub 导航文案统一为 `GitHub`，图标替换为 GitHub Octocat 的内联 SVG。
- CSS 与脚本资源版本号更新为 `homepage-4`，用于避免 GitHub Pages 继续使用旧缓存。

### 2.6 Alpha 实施 03：章节导航占位与 Ripple（2026-09-12）

- 移除“探索/连接”分组眉题，左栏六项栏目直接并列显示。
- 左栏导航加入裁切在控件内的 Ripple；支持鼠标、触摸和键盘点击，并遵守 `prefers-reduced-motion`。
- “学在南雍”加入“大一上、大一下、大二上”顶部切换，每个学期显示“学科导航”和学科 1—5 占位项。
- “玩在金陵”加入“游览导航”和地点 1—5 占位项；“南雍杂忆”加入“时间索引”和 2025、2026 年占位项。
- GitHub 入口改为跳转到 `https://github.com/Mellow-Winds` 的外部链接。

### 2.7 Alpha 修正 01：统一 Ripple 时长与按钮指针（2026-09-12）

- Ripple 动画统一使用 1000ms，鼠标、触摸和键盘触发路径共享同一时长。
- 顶部切换、章节索引和左栏导航按钮统一显示 `pointer` 光标。
- 右侧切换与章节索引重绘后重新挂载 Ripple，避免状态更新截断动画。

### 2.8 Alpha 实施 04：首页个人信息与文章精选（2026-09-12）

- “关于我”视图加入“个人信息”和“文章精选”两个直接可见的模块标题。
- 个人信息模块采用左侧头像预留位、右侧单张个人信息卡片的并列结构；卡片内部只保留 Email、QQ、学校、专业四行，不再嵌套子卡片。
- 文章精选模块直接放置 3 张并列文章卡片，不增加外层卡片容器；当前只使用“文章1—3”结构占位。
- 未提供真实资料前不填充头像、联系方式、学校、专业、文章标题或文章链接；图标全部使用内联 SVG。
- QQ 字段使用已核验的 QQ 品牌 SVG 路径，不再使用消息气泡占位图。

## 3. MD_Demo 设计细则审计结论

### 3.1 视觉北极星

沿用“清晰、清爽、圆润、克制；每一次交互都有可预测反馈”的气质。内容层级由标题、正文、辅助文字和边界建立，不用大段标签、编号或装饰数字制造层级。

### 3.2 颜色合同

默认主题严格使用四个基础色，并通过透明度混合表达次级状态：

```css
--primary: #4A90D9;
--container: #D3E4FD;
--surface: #F9F9FF;
--ink: #1A1B21;
```

实施要求：

- 交互蓝用于主操作、选中态和焦点；容器蓝用于选中背景和强调容器。
- 画布白承载主要内容；文字墨用于正文、轮廓和遮罩。
- 通过 `color-mix()` 或透明度形成 `outline`、`muted`、`soft`、`hover` 等衍生 Token，不另开红、绿、橙等语义色。
- 主按钮使用深色主操作底并配浅色文字，先检查普通文本对比度至少 4.5:1。
- 当前 Alpha 骨架按用户确认使用液态玻璃；渐变只存在于底层背景，表面使用透明度、模糊、高光和阴影表达层级。Windows 透明效果开关不影响材质；高对比度、强制配色或浏览器不支持模糊时，自动使用实体回退。

### 3.3 版式与空间合同

- 桌面端 `body` 使用 `240px minmax(0, 1fr)` 的左右双栏；左栏稳定，右侧主内容独立滚动。
- 主内容设置合理的最大宽度和响应式内边距，避免大屏内容无限拉宽。
- 采用 4px 基础网格，优先使用 4、8、12、16、24、32、48、64px 间距 Token，禁止随意出现无法解释的魔法数字。
- 网格子项必须允许收缩，代码或长文本只能在自己的容器内滚动，不能撑开整页。
- 默认卡片约 20px 圆角，展示容器约 24px 圆角，输入框约 8px 圆角，菜单约 12px 圆角，按钮和标签切换使用胶囊形。
- 卡片只在需要时使用弱边框或单一层级阴影，不为所有元素堆叠阴影。

### 3.4 文字合同

- 字号沿用 12 / 14 / 16 / 20 / 24 / 32px 体系；大屏主视觉最多扩展至 40px，移动端页面标题收敛至 28px。
- 优先系统中文无衬线字体；正文常规字重，标题和当前操作使用中等或半粗字重。
- 标题回答“是什么”，正文回答“为什么”，操作文案直接说明动作。
- 个人经历、学校经历、地点和文章内容必须由用户提供或由已有资料核验，不得用虚构事实填充。

### 3.5 图标合同

- 使用统一的线性 SVG 图形，导航图标目标尺寸 24px，按钮图标目标尺寸 20px。
- 图标通过内联 SVG 或本地 SVG symbol sprite 管理，确保 GitHub Pages 离线部署时不依赖外部图标 CDN。
- 头像、封面等未来内容图片与功能图标区分处理；在用户未提供素材前，使用结构占位，不生成貌似真实的个人信息。
- 图标按钮必须有 `aria-label`；装饰图标设置 `aria-hidden="true"`。

### 3.6 状态、动效和可访问性

- 控件实现默认、悬停、聚焦、按下、禁用、选中和异常等必要状态；状态不能只依赖颜色。
- 焦点使用清晰的可见轮廓；可点击目标至少接近 44px 高度。
- 尊重 `prefers-reduced-motion: reduce`，减少或移除非必要位移和过渡。
- 导航使用语义化链接或按钮，支持键盘操作，并确保移动端仍能访问全部板块。
- 内容区不依赖悬停才能读取；长标题、长摘要、窄屏文字不被裁切。

## 4. Alpha 信息架构

### 4.1 全局骨架

后续实现采用单页静态结构：

```text
┌──────────────────────┬───────────────────────────────────────┐
│ 品牌 / 个人标识       │ 右侧主内容区                           │
│ 六项导航              │ 关于我 → 学在南雍 → 玩在金陵 →          │
│ GitHub 入口           │ 南雍杂忆 → 文章检索 → GitHub            │
└──────────────────────┴───────────────────────────────────────┘
```

桌面端由左侧 `aside` 和右侧 `main` 构成；导航跳转到同页锚点，滚动时同步当前板块状态。移动端隐藏固定左栏，改为顶部品牌栏和可横向滚动的板块导航，保持 `MD_Demo` 的响应式方向。

### 4.2 关于我

定位为首屏入口，承担“我是谁、关注什么、如何继续了解”的信息。计划包含：

- 简短个人定位与一句自述。
- 头像或抽象 SVG 个人标识的占位。
- 一到两个低风险入口，例如查看文章、访问 GitHub。
- 不使用未经用户提供的姓名、学校身份、联系方式或经历。

### 4.3 学在南雍

定位为学习经历与思考的内容区。计划使用时间线、主题卡片或分组列表承载课程、阅读、项目、方法论等内容；具体条目由用户提供后再定稿。视觉上使用 `MD_Demo` 的容器蓝强调重要节点，正文保持画布白和文字墨。

### 4.4 玩在金陵

定位为南京城市生活与兴趣探索区。计划使用地点/路线/活动卡片，统一采用 SVG 地图或兴趣图标；地点描述、照片和推荐理由必须来自用户内容或明确标注为待补资料，不把常识性南京景点自动写成个人经历。

### 4.5 南雍杂忆

定位为碎片化记忆、短札或片段记录区。计划支持日期、标题、摘要和阅读入口；内容以轻量卡片或纵向记忆流呈现，避免过度装饰和编号化。

### 4.6 文章检索

定位为全站文章的静态检索入口。Alpha 采用前端本地数据，不建立后端接口：

- 文章元数据集中维护在静态 JS/JSON 数据中。
- 关键词匹配标题、摘要、标签和正文索引字段。
- 输入时显示结果数量；无结果时说明下一步，例如清空关键词或调整筛选。
- 文章详情可使用同页展开、锚点或静态独立页面；先根据文章数量选择最简单且可维护的方式。
- 如果加入标签筛选，筛选项只来自真实文章数据，不能预置虚假的分类内容。

### 4.7 GitHub

定位为外部连接区：

- 展示 GitHub 用户名、简介和个人主页链接的内容位。
- 以静态链接展示精选仓库或项目列表。
- Alpha 不强依赖实时 GitHub API，避免 API 限额、跨域和 GitHub Pages 访问不稳定；如未来需要实时数据，必须提供静态回退。
- GitHub 标识使用本地 SVG，沿用 `--ink` 体系，不另引入品牌色。

## 5. 后续实现方案

### 5.1 技术边界

- 首选原生 HTML、CSS、JavaScript，无框架、无构建步骤、无后端。
- 根目录保留 `index.html`，资源使用相对路径，兼容 GitHub Pages 的项目子路径。
- 不使用服务端路由；优先使用页面内锚点和必要的 hash 状态，确保刷新和直接访问链接可用。
- 不引入外部字体、图标 CDN 或运行时必须联网的依赖。

### 5.2 建议目录

```text
MyBlog/
├── index.html
├── assets/
│   ├── css/
│   │   ├── tokens.css       # 四色 Token、字号、间距、圆角
│   │   ├── layout.css       # 左右双栏、主内容和响应式骨架
│   │   └── components.css   # 卡片、按钮、输入、检索、状态
│   ├── js/
│   │   ├── app.js           # 导航、滚动状态和页面初始化
│   │   ├── content.js       # 可替换的个人内容与文章元数据
│   │   └── search.js        # 静态文章检索
│   └── icons.svg            # 统一 SVG symbol sprite
├── devlog/
│   └── alpha_plan.md
├── .gitignore
└── AGENTS.md                # 本地文件，已被忽略
```

目录只是 Alpha 的实现建议；开始写代码前，如果用户指定技术栈、现有素材或文件组织，以上方案以用户的新要求为准。

### 5.3 组件优先级

1. 全局 Token、左右双栏和响应式导航。
2. SVG 图标注册与可访问名称。
3. 关于我首屏和六个板块的语义结构。
4. 复用卡片、标签、按钮、时间线、文章列表等基础组件。
5. 本地文章检索、筛选、无结果状态。
6. 内容占位替换、链接校验和 GitHub 区块。
7. 视觉回归、可访问性检查和 GitHub Pages 交付说明。

## 6. 验收标准

### 6.1 仓库与静态托管

- [x] `MyBlog` 已是独立 Git 仓库。
- [x] 默认分支为 `main`。
- [x] `.gitignore` 已忽略 `AGENTS.md`、依赖目录和生成物。
- [x] `devlog/alpha_plan.md` 保持可提交；`devlog/` 不加入忽略，方便后续保留计划和日志。
- [x] 已配置 GitHub remote；GitHub Pages 从 `main` 根目录发布的后台配置待确认。
- [ ] 页面不依赖 Node 服务或服务器端渲染，直接打开静态入口也能读取主体结构。

### 6.1.1 Alpha 01 材质兼容修正

- 页面入口显式声明 `data-material-mode="liquid-glass"`，与 `MD_Demo` 的材质状态合同保持一致；即使脚本未执行，CSS 仍会按液态玻璃渲染。
- `assets/js/app.js` 负责绘制稳定的底层渐变，仅监听高对比度和强制配色偏好；这些偏好开启时移除内联渐变，避免覆盖对应回退。
- 支持 `backdrop-filter` 的浏览器使用半透明、背景模糊、饱和度、高光和阴影；Windows 关闭透明效果时仍保留材质，不支持模糊时使用实体表面。
- 兼容性目标是所有设备均可正常阅读和操作；不把“所有设备都必须显示模糊”作为不现实的验收条件。

### 6.2 视觉与响应式

- [ ] 左右双栏在桌面端稳定呈现，左栏导航宽度和层级接近 `MD_Demo`。
- [ ] 页面使用四个基础色及其透明度衍生色，没有未经说明的额外色板。
- [ ] 字号、间距、圆角、卡片边界和按钮形态遵循 `MD_Demo` 合同。
- [ ] 六个板块均可从导航到达，当前板块反馈清晰。
- [ ] 至少检查 1440×900、1024×768、768×1024、390×844、320×800。
- [ ] 320px 宽度无整页横向溢出；长文本和文章列表可以收缩。

### 6.3 图标与交互

- [ ] 所有功能图标均为内联 SVG，无 emoji 和图标字体；GitHub 使用官方标志形态。
- [ ] 纯图标按钮有可访问名称，装饰图标不重复朗读。
- [ ] 导航、文章检索、筛选和外链具备键盘路径。
- [ ] 交互有默认、悬停、聚焦、按下、禁用和选中等必要反馈。
- [ ] 减少动态效果设置生效，重要状态不只由动画传达。

### 6.4 内容真实性

- [ ] 个人姓名、头像、经历、地点记忆、文章和 GitHub 地址均来自用户提供或可核验资料。
- [ ] 未提供的个人字段明确标记为待填，不使用虚构示例冒充真实资料。
- [ ] GitHub 实时数据即使加载失败也不会破坏静态页面主体。

## 7. 待用户补充的信息

这些信息不影响本轮规划，但在进入代码阶段前需要确认：

1. 主页显示名称、个人简介和头像/标识素材。
2. “学在南雍”“玩在金陵”“南雍杂忆”的真实内容、排序和希望隐藏的字段。
3. 首批文章标题、摘要、标签、日期、正文及文章详情形式。
4. GitHub 用户名、主页 URL、希望展示的仓库和仓库链接。
5. GitHub 仓库名称及远程 URL，以便配置 `origin` 并完成 Pages 发布。
6. 是否允许使用照片等非图标视觉素材；无素材时默认仅使用 SVG 图形和文字。

## 8. Git 工作流

- 当前仓库已纳入计划文件、静态入口、液态玻璃样式和材质初始化脚本；`AGENTS.md` 按约定忽略。
- 进入代码阶段后，按“Token/骨架 → 板块 → 检索 → 适配与无障碍 → 发布”拆分小提交。
- 每次提交前执行 `git diff --check`，并如实记录未完成项和验证结果。
- 不配置未经用户提供的 remote，不执行删除或覆盖现有资料的操作。

## 9. 个人主页首页计划与实施记录

### 9.1 参考调研与取舍

本轮读取了几个真实的个人博客主页，用于提炼首页信息组织方式。调研只用于形成结构判断，不复制站点内容、品牌或代码：

1. [Paul Stamatiou](https://paulstamatiou.com/)：首页直接并列个人身份信息、文章列表、器材兴趣和照片入口。可借鉴“个人信息 + 内容入口”并置的结构，但本项目先收敛为个人信息卡片和文章精选，不加入照片墙。
2. [Maggie Appleton](https://maggieappleton.com/)：使用少量清晰的一级内容分类承载 Garden、Essays、Notes、Patterns 等不同内容。可借鉴分类入口的清晰度，避免为 MyBlog 提前增加过多栏目。
3. [Simon Willison’s Weblog](https://simonwillison.net/)：将 About、Subscribe、TILs、Tools 与 Entries、Links、Quotes、Notes 等内容入口分开，并提供站内搜索。可借鉴“功能入口与内容入口分离”的信息架构，文章检索后续再接入。
4. [Gwern.net](https://gwern.net/)：拥有非常强的主题分类、时间索引和文章目录，适合作为检索和长期知识库的参考；其信息密度较高，本项目首页不直接采用这种密集目录。
5. [Overreacted](https://overreacted.io/)：首页将个人博客标识与文章标题、日期、短摘要组成极简列表。可借鉴精选文章的直接呈现方式，但本项目先使用三张独立卡片，保留个人主页的空间感。

调研环境说明：项目要求的 `wmux browser` 命令在当前环境不可用，因此本轮改用网页访问工具读取上述公开主页；未因此修改项目代码。

### 9.2 首页目标

首页默认对应左栏“关于我”，只呈现三类内容：

1. 头像预留。
2. 个人信息卡片：Email、QQ、学校、专业。
3. 文章精选：预留 3 个文章卡片位置。

首页不加入长篇自我介绍、口号、统计数字或未经用户提供的身份信息。栏目名称已经由左栏表达，右侧只保留必要标题和内容本体。

### 9.3 首页结构计划

```text
右侧主内容区
├── 个人信息区
│   ├── 头像预留位
│   └── 个人信息卡片
│       ├── Email
│       ├── QQ
│       ├── 学校
│       └── 专业
└── 文章精选区
    ├── 文章卡片 1
    ├── 文章卡片 2
    └── 文章卡片 3
```

桌面端个人信息区与文章精选区形成清晰的上下层级；个人信息卡片内部使用四行信息，文章精选使用三张等宽卡片。右侧不恢复覆盖整栏的大卡片容器，各内容模块独立拥有必要的表面和边界。

### 9.4 内容与占位规则

- 头像只预留尺寸、裁切比例和替换入口；用户未提供头像前不生成貌似真实的照片。
- Email、QQ、学校、专业当前使用明确标注语义的测试占位值：`test@test.com`、`123456`、`测试学校`、`测试专业`；真实值等待用户提供。
- 文章精选只预留 3 个稳定位置；文章标题、日期、摘要、标签和链接等待用户提供，不使用虚构文章冒充真实内容。
- 个人信息四项使用统一的内联 SVG 图标：邮件、QQ 品牌、学校、专业；不引入图标字体或外部 CDN。
- 文章卡片优先显示标题和必要元信息；若没有真实摘要，则不补写解释性占位段落。

### 9.5 视觉与交互计划

- 延续现有 `MD_Demo` 设计合同和液态玻璃材质：页面背景提供光斑与层次，内容表面使用透明度、模糊、高光和阴影。
- 头像预留位使用独立圆形或圆角结构，不把空头像扩大成视觉主卡片。
- 个人信息卡片采用弱边界、统一图标尺寸和至少 44px 的可点击行高；Email 若提供真实地址，再转为 `mailto:` 链接，QQ 默认只展示字段，除非用户明确提供跳转方式。
- 文章精选卡片使用统一高度、标题截断策略和明确的悬停/聚焦/按下状态；卡片整体可点击时必须有真实文章链接。
- 动效继续使用统一 Ripple 规范；减少动态效果时停止非必要动画，但不隐藏重要信息。
- 遵循精简原则：不添加重复栏目名、眉题、宣传语、说明句或“这里是……”式占位文案。

### 9.6 实施顺序

1. 先确认个人主页默认视图和头像预留尺寸。
2. 在现有右侧内容区加入个人信息模块结构，不改动左栏导航和液态玻璃基础材质。
3. 加入四项 SVG 信息图标与待填字段状态。
4. 加入三篇文章精选卡片的静态数据结构和空链接保护。
5. 完成桌面、平板和移动端排版，检查长邮箱、长专业名称和空字段不撑破页面。
6. 进行视觉回归、键盘操作、Ripple 时长、焦点状态和 GitHub Pages 资源路径检查。

### 9.7 后续需要用户提供

1. 头像文件或头像暂不使用的确认。
2. Email、QQ、学校和专业的真实内容，以及哪些字段需要点击跳转。
3. 三篇精选文章的标题、摘要/简介、日期、标签和链接。
4. 文章卡片是否允许使用封面；若允许，需要用户提供图片或明确允许的素材来源。

### 9.8 首页重设计计划（2026-09-13，待用户确认）

本节以用户最新需求为准，覆盖并替换前一版“右侧两块卡片”的首页实施方案；本轮只完成计划，不提前写网站代码。

#### 9.8.1 参考提炼

- [Apple iPhone 17](https://www.apple.com/iphone-17/)：借鉴“首屏建立主题、继续滚动逐章节展开、每段只突出一个重点”的叙事节奏，以及大留白、强层级和内容随滚动渐进出现的感觉；不复制 Apple 的品牌、产品素材、文案或页面代码。
- [Sunshine's Blog / mingshuo.org](https://mingshuo.org/)：借鉴个人博客把个人介绍、最新文章、精选项目、外部入口分成清晰内容区的组织方式；不复制其个人资料、文章、项目或统计数字。
- `MD_Demo`：继续遵循清晰、清爽、圆润、克制的设计合同，保留本项目已经确认的四基础色、液态玻璃及无障碍回退策略。Apple 的长页叙事只改变首页信息节奏，不改变本项目的视觉基础。

#### 9.8.2 首页信息结构

```text
首页（#home）
├── 首屏格言
│   ├── 遇事不怒，吃饱睡足；
│   │   读万卷书，行万里路
│   └── 小字与箭头：欢迎来到Mellow的Blog。与我一起，在风与歌中成长吧~
├── 个人资料
│   ├── 头像/个人标识
│   └── 已核验的个人字段
├── 项目
│   └── 项目卡片或列表：名称、状态、简短说明、访问入口
├── 文章
│   └── 最新/精选文章列表：标题、日期、分类或标签、必要摘要
└── 收束入口
    └── GitHub 或其他已确认外部链接
```

栏目名称只在必要的区块标题出现；不额外添加眉题、宣传语、统计数字或解释性占位段落。已有底部导航继续负责站点级跳转，首页内部用自然纵向顺序呈现内容，不再复制一套首页目录。

#### 9.8.3 首屏与滚动行为

1. 点击底部导航“首页”或访问 `#home` 时，主内容滚动位置回到顶部，第一视口只承担格言展示。
2. 首屏使用接近一屏的高度，格言在视觉中心附近；下方只保留用户确认的小字“欢迎来到Mellow的Blog。与我一起，在风与歌中成长吧~”和线性 SVG 箭头。
3. 用户继续滚动时，格言与背景光斑轻微退场，个人资料从下方进入；之后依次进入项目和文章。使用原生滚动，不锁定滚轮、不强制分页、不把滚动速度交给脚本。
4. 过渡只使用 `transform`、`opacity` 和必要的材质变化；滚动事件通过 `requestAnimationFrame` 合并更新，进入视口的区块用 `IntersectionObserver` 或等价机制触发。
5. 动效必须可被用户中途打断并从当前呈现状态继续；`prefers-reduced-motion: reduce` 下移除位移、视差和循环动画，保留静态层级、焦点和内容顺序。

#### 9.8.4 内容数据约定

- 个人资料沿用现有字段结构，但当前工作区中的姓名、联系方式、学校、专业和头像均视为待确认内容；没有用户确认时不把它们包装成最终个人事实。
- 新增 `projects` 数据区，建议字段为 `title`、`status`、`summary`、`tags`、`url`、`repository`；项目卡片只有在存在真实链接时才整体可点击。
- 新增 `articles` 数据区，建议字段为 `title`、`date`、`category`、`summary`、`url`；首页只展示精选/最新条目，完整列表仍由现有“文章检索”入口承担。
- 项目、文章、标签、日期和链接全部来自用户提供或仓库内可核验资料；不使用“项目 1”“文章 1”作为最终页面内容，也不以参考站点内容代替用户内容。
- 如果暂时没有真实项目或文章，保留结构和空数据状态，但不在访客页面显示虚构卡片。

#### 9.8.5 视觉实现边界

- 只改造 `#home` 的渲染结构、首页样式和首页滚动交互；`学在南雍`、`玩在金陵`、`南雍杂忆`、`文章检索` 和 GitHub 外链的既有行为不随意改写。
- 继续复用 `assets/css/style.css` 的颜色、间距、材质和可访问性变量；除非实现确实需要，不拆分现有 CSS/JS 文件。
- 桌面端采用宽松的内容节奏和更大的格言字号；平板端收紧左右留白；移动端保证格言不被底部导航遮挡，项目与文章改为单列。
- 不引入外部字体、图标 CDN、远程运行时或 Apple 页面资源；功能图标继续使用本地/内联 SVG。

#### 9.8.6 实施顺序

1. 确认“向下滚动”小字、个人资料字段、首批项目和文章数据，以及是否使用真实头像/封面。
2. 在 `renderAbout()` 中建立语义化首页区块和真实数据接口；保留现有 hash 路由及其他页面结构。
3. 实现首屏格言、滚动区块层级、底部导航安全区和移动端布局。
4. 实现滚动渐进动效、区块可见状态、焦点/按下状态和减少动态效果回退。
5. 接入已确认内容，检查链接、长标题、空数据和 GitHub Pages 相对路径。
6. 做真实浏览器视觉回归后再交付，不以源码检查代替滚动体验验收。

#### 9.8.7 验收标准

- 点击首页后第一视口清晰出现完整格言，格言文字严格为“遇事不怒，吃饱睡足；读万卷书，行万里路”。
- 格言下方只有低干扰的小字/箭头提示；向下滚动后按“个人资料 → 项目 → 文章”的顺序自然出现。
- 首页不显示未经确认的个人事实、项目、文章或统计数据。
- 滚动体验保持原生可控，快速滚动、触摸、键盘 PageDown/空格及减少动态效果设置均可正常使用。
- 首页不会被固定底部导航遮挡；320px 宽度无整页横向溢出，长标题与长链接可换行或在局部容器内处理。
- 现有其他栏目、学习资料阅读器、深链接、后退/前进和 GitHub 外链不回归。
- 至少检查 1440×900、1024×768、768×1024、390×844、320×800，并执行 `git diff --check`；当前环境 `wmux` 命令不可用，真实浏览器检查需在可用浏览器面板的环境中补做。

### 9.9 用户确认的首版内容（2026-09-13）

- 首屏小字确定为：`欢迎来到Mellow的Blog。与我一起，在风与歌中成长吧~`。
- 个人资料保持当前首页已有状态，不新增字段，不改写现有资料表现。
- 首个项目使用卡片加圆角玻璃按钮呈现：

  ```text
  NJU-Hub
  开发中：持续更新
  [点击访问]
  https://github.com/Mellow-Winds/NJU-Hub
  ```

- 项目卡片不补充未经提供的项目介绍、技术栈、数据或统计；按钮跳转到上述 GitHub 地址。
- 文章区先保留“文章”内容位置，等待真实文章数据后再呈现，不使用虚构标题或摘要。

### 9.10 首页计划修订（2026-09-13，覆盖 9.8—9.9 的展示细节）

本节根据用户提供的界面截图和最新要求修订。附件只作为视觉参考，不把其中的“个人信息”“文章精选”、文章 1—3 等文字视为最终页面内容。

#### 9.10.1 首屏

```text
                         MellowBlog

              遇事不怒，吃饱睡足；
              读万卷书，行万里路

        欢迎来到Mellow的Blog。与我一起，在风与歌中成长吧~

                              ︾
```

- 首屏不显示“首页”“个人信息”等解释性大标题。
- 欢迎文案使用用户确认的原文；不再显示“向下滚动”文字。
- `︾` 以线性 SVG V 形符号实现，不使用字符字体或 emoji。
- V 形符号只做轻微上下波动，幅度小、频率低，不抢夺格言注意力；用户开始滚动后逐渐隐藏。

#### 9.10.2 个人资料

- 视觉上复现当前“个人信息”页面的内容状态：左侧圆形头像，右侧半透明玻璃资料卡。
- 资料卡保留当前四项：Email、QQ、学校、专业；图标、排列和文字权重沿用现有状态。
- 删除页面中的“个人信息”大标题，改由头像、资料卡和上下留白自然建立层级。
- 桌面端保持头像与资料卡横向排列；移动端改为纵向排列，资料字段继续保持清晰可读。

#### 9.10.3 项目

- 不显示“项目”大标题，个人资料卡之后直接进入项目卡片。
- 首个项目只呈现已确认内容：

  ```text
  NJU-Hub
  开发中：持续更新
  [点击访问]
  ```

- 卡片使用现有液态玻璃语言；按钮使用圆角玻璃按钮，链接为 `https://github.com/Mellow-Winds/NJU-Hub`。
- 不增加项目介绍、技术栈、统计数字或未经确认的说明。

#### 9.10.4 文章

- 不显示“文章精选”大标题。
- 文章内容沿纵向顺序接在项目之后；有真实文章时直接呈现文章卡片或列表。
- 在真实文章数据接入前，不显示“文章1”“文章2”“文章3”等占位内容。

#### 9.10.5 动画行为

1. 首页进入：格言直接成为视觉焦点，欢迎文案轻微淡入；不使用启动页或额外介绍。
2. V 形符号：以约 4—6px 的小幅度上下运动提示阅读方向；不做夸张弹跳，不持续制造注意力竞争。
3. 开始滚动：V 形符号隐藏，格言向上轻移并降低透明度；个人资料从下方自然进入。
4. 资料卡进入：头像和玻璃卡片同步显现，保持同一空间关系，不拆成多段花哨动画。
5. 项目卡片进入：卡片以轻微透明度、位移和材质清晰度变化呈现；按钮不延迟到卡片之后才出现。
6. 所有滚动效果使用原生滚动，动画可被快速滚动、触摸或键盘操作立即打断。
7. `prefers-reduced-motion: reduce` 下取消 V 形波动、位移和视差，只保留静态 V 形符号或直接隐藏，并保留全部内容。

#### 9.10.6 明确禁止

- 不显示“个人信息”“文章精选”“项目”等现有大标题。
- 不增加眉题、宣传语、解释句、统计数字或装饰性说明。
- 不使用文章 1—3、项目 1 等虚构占位内容。
- 不复制附件中的头像、文章文字或其他素材；现有头像仅沿用项目当前资源。
- 不改变其他栏目和学习资料阅读页的已有结构。

#### 9.10.7 验收标准

- 首页第一视口只有品牌识别、格言、欢迎小字和 V 形引导符号。
- V 形符号确实轻微波动，用户滚动后隐藏；减少动态效果设置有效。
- 个人资料仍能看出当前页面的头像、Email、QQ、学校和专业，但不再出现“个人信息”大标题。
- 项目卡片只呈现 NJU-Hub、开发状态和“点击访问”玻璃按钮。
- 页面通过留白和内容顺序建立层级，不依赖大标题解释页面结构。

### 9.11 首页实施（2026-09-13）

- 最新确认覆盖此前标题约定：文章区显示居中的“最近文章”；个人资料直接复用现有头像、四项资料与卡片结构，仅移除区块大标题。
- 已实现首屏两行格言、欢迎小字、单 V 形 SVG 引导（上下 6px、2.4 秒周期），可点击向下浏览；滚动后隐藏引导。
- 下方依次为原个人资料、NJU-Hub 卡片与圆角玻璃访问按钮、最近文章；尚无真实文章，移除文章 1—3 占位数据。
- 首页动效随主内容原生滚动更新；系统减少动态效果时停止位移和引导波动。首页重复点击可回到顶部。
- 通过 app.js 语法检查、学习导航与阅读器现有回归测试、修改文件差异格式检查。
- 当前环境未提供 wmux 命令，尚未完成实际浏览器截图和滚动视觉验收。

### 9.12 首页问题细化与分页式滚动计划（2026-09-13，待实施）

本节根据用户提供的两张截图和最新反馈制定，附件只作为尺寸、空间和视觉关系参考，不把截图中的占位内容视为页面数据。

#### 9.12.1 当前问题判断

1. 项目卡片使用了接近主内容宽度的“大容器”比例，项目本身的信息量却很少，视觉重量与内容价值不匹配。
2. 首屏、个人资料、项目和最近文章目前属于同一条连续内容流，用户可以停在任意中间位置，无法感受到“从首页进入个人资料”的明确场景变化。
3. 首页有材质和内容，但缺少稳定的节奏：首屏结束后没有清晰落点，项目区也没有形成轻量收束，最近文章没有真实内容时会显得空。
4. “少即是多”要求首页减少解释性标题和大面积卡片，把层级交给视口高度、留白、卡片尺寸和滚动位置。

#### 9.12.2 首页场景结构

首页改为四个纵向场景，每个场景占据一个视口落点：

```text
场景 1：格言首屏
         格言 + 欢迎小字 + V 形引导

场景 2：个人资料
         现有头像 + 现有资料卡，保持现有内容

场景 3：项目
         小尺寸项目卡横向排列
         NJU-Hub + 更多项目鬼点子生成中...

场景 4：最近文章
         居中的“最近文章”
         后续接入真实文章内容
```

场景之间使用 `scroll-snap-type: y mandatory` 和 `scroll-snap-align: start`，每个场景至少占 `100svh`；项目和文章不会停在两个场景的中间。桌面端和移动端都保持同一纵向落点，底部导航占用的安全区域计入场景内边距。

#### 9.12.3 项目区域修订

- 项目卡片的最大宽度控制在现有大卡片约三分之一以内，建议桌面端单卡约 240—300px 宽、150—190px 高，具体随视口收缩。
- 桌面端从左到右排列，卡片之间使用稳定间距；不再使用一张横跨内容区的大卡片。
- 第一张卡片只保留：`NJU-Hub`、`开发中：持续更新`、`点击访问`。
- 第二张使用同等尺寸的轻量收束卡片，文字为：`更多项目鬼点子生成中...`。
- 第二张不设置虚构链接、不添加按钮；它承担内容未完待续的状态提示。
- 移动端改为横向可滑动卡片带，卡片仍保持小尺寸；滚动项目卡时不触发首页纵向场景切换，避免横纵手势冲突。
- 项目场景不额外显示“项目”大标题；卡片本身承担识别作用。

#### 9.12.4 滚动交互规则

1. 首页初始位置固定在格言场景顶部。
2. 用户向下滚动一次，页面落到个人资料场景；资料卡完整进入视口，不允许停在格言与资料卡之间。
3. 用户向上滚动一次，页面回到格言场景；反向路径和进入路径保持一致。
4. 继续向下分别落到项目场景和最近文章场景。
5. 鼠标滚轮、触摸滑动、触控板和键盘 PageDown/PageUp 都使用同一组场景落点。
6. 页面不锁死连续滚动，也不拦截正常触摸；`scroll-snap-stop: always` 只用于避免一次手势跳过关键场景。
7. `prefers-reduced-motion: reduce` 下保留场景落点和内容顺序，取消格言位移、V 形波动和场景渐入。

#### 9.12.5 首页缺失内容的处理

首页当前缺少的不是更多介绍文字，而是三个可感知的关系：

- 首屏与资料之间的明确入口：由全屏场景和 V 形引导完成。
- 项目之间的数量感：由小卡片横向排列和“更多项目鬼点子生成中...”完成。
- 首页与长期内容之间的出口：由“最近文章”场景承接，真实文章数据接入后直接呈现标题列表或小卡片。

不新增统计数字、个人简介、友情链接等解释性模块，除非用户后续提供对应内容。

#### 9.12.6 实施顺序

1. 将首页 DOM 重组为四个 `home-screen` 场景，保留个人资料的原有字段和图标。
2. 给首页滚动容器增加只在 `home-page` 生效的纵向吸附规则，并处理底部导航安全区。
3. 将项目卡片改为紧凑横向卡片，加入“更多项目鬼点子生成中...”收束卡片。
4. 把最近文章标题固定在第四个场景中央，接入真实文章前保持内容区干净。
5. 调整 V 形引导、首屏退场和资料场景进入动效，让中断、反向滚动和减少动态效果都保持稳定。
6. 验证桌面、平板和移动端的场景落点、横向项目卡和底部导航遮挡问题。

#### 9.12.7 验收标准

- 项目卡片视觉面积不超过当前大卡片约三分之一，桌面端可从左到右排列。
- 项目区最后固定出现“更多项目鬼点子生成中...”。
- 从首屏向下滚动后，个人资料卡完整落入视口；向上滚动完整回到首屏，不出现资料卡悬在中间的状态。
- 四个首页场景均有明确吸附落点，滚轮、触摸、触控板和键盘操作不跳过关键场景。
- 个人资料字段、头像、图标和资料卡样式保持现有状态。
- “最近文章”居中出现，真实文章接入前不显示虚构文章卡片。
- 320px 宽度无横向溢出；移动端项目卡横向滑动不干扰首页纵向滑动。
- 通过真实浏览器检查后再确认视觉完成；当前环境仍未提供 `wmux` 命令，现阶段只能完成源码级和现有回归检查。

### 9.13 首页结构二次修订（2026-09-13）

本节覆盖 9.12 中“四个完整场景”的表述，改为用户最新确认的“两层场景”。

#### 9.13.1 正确的场景关系

```text
场景 1：独立首屏
└── 格言、欢迎小字、V 形引导

场景 2：连续内容场景
├── 个人资料
├── 项目
│   ├── NJU-Hub 小卡片
│   └── 更多项目鬼点子生成中...（纯文字）
└── 最近文章
```

- 只有场景 1 使用全屏场景落点。
- 从场景 1 向下滚动时，页面一次进入场景 2 的顶部，个人资料完整出现。
- 场景 2 内部使用正常连续滚动；个人资料、项目和最近文章之间不再分别吸附，也不强制每一块占满视口。
- 向上滚动到场景 2 的顶部后，再继续向上才回到场景 1。
- 这样保留首屏与内容区的明确边界，也保留内容区内部自然阅读的节奏。

#### 9.13.2 项目区修订

- 桌面端项目卡片缩小至当前大卡片约三分之一，采用横向排列。
- NJU-Hub 使用小型玻璃卡片，保留名称、开发状态和访问按钮。
- `更多项目鬼点子生成中...` 放在项目卡片之后或卡片行末，使用普通文字呈现。
- 该文字不包裹卡片、不使用边框、不使用背景、不设置按钮或链接。
- 移动端项目卡片允许横向排列或横向滑动；提示文字保持普通文本，不参与卡片滑动。

#### 9.13.3 首页缺失内容的判断方式

目前已经确定的内容是：首屏、个人资料、项目、最近文章。个人主页还需要一个“身份闭环”，但不需要增加长篇介绍。

建议将缺失内容分为三类：

1. 必须有的身份信息

   当前资料卡有联系方式、学校和专业，但缺少一句最短的身份定位。它可以是姓名/昵称与方向的组合，例如：

   ```text
   Mellow
   软件工程 · 经济
   ```

   这部分需要使用用户确认的文字，不能擅自编写个人简介。

2. 必须有的内容出口

   最近文章承担持续更新入口；项目承担作品入口；GitHub 和 Email 已经可以承担联系与外部连接，不再额外增加友情链接、统计数字或社交平台列表。

3. 必须有的页面收尾

   连续内容场景底部需要一个轻量收束位置，可以只放 GitHub、Email 或版权信息中的已确认内容。它的作用是让页面自然结束，不增加新的内容板块。

#### 9.13.4 推荐的最小首页闭环

```text
首屏格言
↓
身份与个人资料
↓
项目与持续生成提示
↓
最近文章
↓
GitHub / Email / 页面收尾
```

当前最可能缺少的是“身份定位”和“页面收尾”，而不是更多卡片。它们都应保持短、静、直接呈现。

#### 9.13.5 后续实现边界

- 将首页重组为 `home-hero` 和 `home-content` 两个顶层区域。
- 只对 `home-hero` 使用首屏高度与场景边界。
- `home-content` 按普通文档流排列个人资料、项目和最近文章。
- 项目卡片改小并横向排列，生成提示改为无容器文字。
- 个人资料字段与现有内容保持不变；身份定位和页面收尾只有在用户提供或确认后加入。

### 9.14 资料卡扩展与两场景实施

- 用户确认在原资料卡四项信息下方加入自我介绍，当前使用“测试”重复 100 次，共 200 字；无额外介绍标题。沿用原头像与四项字段。
- 首页仅保留首屏和连续内容区两个吸附区域，内容区包含资料、项目、最近文章；边界滚轮和 V 按钮可进入资料区，内容区内部正常滚动。
- 项目卡宽 250px、内边距 24px，横向排列并允许窄屏换行；“更多项目鬼点子生成中...”为卡片外普通文字。
- 不新增 GitHub 收尾模块，沿用底部导航入口。
- 已通过脚本语法、200 字占位与容器闭合检查、现有学习导航和阅读器回归测试。wmux 仍不可用，未完成真实浏览器视觉及触控吸附验收。

### 9.15 最近项目与文章精选计划（2026-09-13，待实施）

#### 9.15.1 内容区结构

场景 2 内部按以下顺序呈现：

```text
个人资料卡 + 200 字自我介绍
↓
最近项目
  NJU-Hub 小卡片
  更多项目鬼点子生成中...（普通文字）
↓
文章精选（居中）
  二分查找的『红绿算法』
  二位前缀和
  知识点总结
```

- “最近项目”恢复为明确的大字号区块标题，用于让访客知道下面卡片的内容类型。
- “文章精选”作为文章区的居中标题，替代“最近文章”。
- 个人资料内容保持现有四项资料、头像和 200 字测试介绍，不修改字段内容。
- 不增加 GitHub 收尾，底部导航已经提供 GitHub 入口。

#### 9.15.2 项目展示

- NJU-Hub 卡片保持紧凑尺寸，桌面端从左到右排列。
- 卡片显示 `NJU-Hub`、`开发中：持续更新` 和圆角玻璃按钮 `点击访问`。
- `更多项目鬼点子生成中...` 放在卡片排列之后，以普通文字显示，不使用卡片、边框、背景或按钮。
- 项目区允许未来追加更多真实项目；单个项目卡尺寸保持统一，不重新扩张为横跨内容区的大卡片。

#### 9.15.3 文章数据

文章精选固定使用以下三篇仓库内真实 Markdown 文件，页面标题去掉 `.md` 后缀：

| 页面标题 | 来源显示 | 文件路径 |
| --- | --- | --- |
| 二分查找的『红绿算法』 | 大一上-C Programming-算法 | `大一上/C Programming/算法/二分查找的『红绿算法』.md` |
| 二位前缀和 | 大一上-C Programming-算法 | `大一上/C Programming/算法/二位前缀和.md` |
| 知识点总结 | 大一下-软工I-期末复习 | `大一下/软工I/期末复习/知识点总结.md` |

单张文章卡片只呈现：

```text
二位前缀和
大一上-C Programming-算法
```

来源文字使用较小的辅助字重和低对比度颜色；卡片不显示文章摘要、统计数字或额外解释。点击后复用现有学习阅读器的深链接打开对应 Markdown 文件。

#### 9.15.4 场景与滚动

- 场景 1 仍是唯一的全屏首屏。
- 进入场景 2 后，资料、最近项目和文章精选属于同一条普通内容流。
- 内容区只在首屏边界进行一次落点处理，内部不设置分段吸附；文章卡片可以自然停留在视口中。
- 文章区使用正常纵向排列，桌面端三列，移动端单列。
- 文章卡片进入时使用轻微透明度和位移变化；快速滚动、反向滚动和减少动态效果设置都能立即接管。

#### 9.15.5 验收标准

- 页面清晰显示“最近项目”大标题。
- 页面清晰显示居中的“文章精选”标题。
- 三篇文章标题和来源与仓库真实路径一致。
- 文章卡片只显示标题和来源，不出现摘要或虚构数据。
- NJU-Hub 项目卡保持紧凑；“更多项目鬼点子生成中...”没有卡片容器。
- 个人资料和 200 字测试介绍保持现有内容。
- 其他导航、学习阅读器和 GitHub 底部入口不受影响。

#### 9.15.6 实施记录（2026-09-13）

- 已在 `assets/js/app.js` 写入三篇真实文章的标题、来源和阅读器深链接；路径中的 `/` 使用 URL 编码，避免路由拆分。
- 已将项目区改为“最近项目”标题 + 紧凑项目卡 + 卡片外生成提示，并将文章区标题改为居中的“文章精选”。
- 文章卡片显示标题与来源；标题和来源均允许任意位置换行，长路径不会被截断或撑破卡片。
- 已更新 `assets/css/style.css` 与入口版本号，保留个人资料、底部导航和现有阅读器实现。
- 已通过 `node --check assets/js/app.js`、学习导航回归、阅读器回归和 `git diff --check`；wmux 浏览器命令当前不可用，暂未完成真实浏览器视觉检查。

## 10. 学在南雍目录化实施记录（2026-09-12）

- 左栏品牌名称更新为 `MellowBlog`，品牌图标与个人主页头像统一读取 `icon/icon128.png`。
- `docs_learning` 建立 `大一上`、`大一下`、`大二上` 三个年级目录；每个年级下建立 `课程1`—`课程5` 学科目录，并用 `.gitkeep` 保留空目录。
- 学在南雍顶部切换与课程卡片由目录树生成。GitHub Pages 部署后通过 GitHub Contents/Git Tree API 读取公开仓库的 `docs_learning`，新建并提交年级或学科目录后，页面刷新或定时刷新即可同步；本地文件预览保留默认目录作为回退。
- 课程卡片左下角显示该学科目录内的内容文件数量，`.gitkeep` 不计入内容；点击课程卡片进入独立课程占位界面，暂不接入 Markdown 摘要导航。
- 课程占位界面沿用主页的液态玻璃双栏壳层；左栏隐藏站点导航，仅显示 `MellowBlog` 与 `年级-课程` 上下文，右栏保留正文占位区。
- 目录同步使用 `cache: no-store` 并在页面初始化及每 60 秒重新读取 GitHub 默认分支树；未提交到远程的本地目录不会出现在 GitHub Pages，新增目录仍需至少包含一个可跟踪文件。
- 本地预览新增零依赖服务器：运行 `npm run dev` 后访问 `http://127.0.0.1:4173/`，页面每秒读取本地 `docs_learning` 目录，因此本地新增年级或课程目录无需改动 JavaScript 即可同步；空年级也会保留顶部按钮。
- 当前本地新增目录 `docs_learning/3/2` 已验证可生成年级按钮 `3` 与课程卡片 `2`。
- 本轮不改动玩在金陵、南雍杂忆、文章检索和 GitHub 入口的既有行为。

## 11. 课程 Markdown 阅读页（2026-09-12）

- 课程左栏顶端固定为圆角 SVG 返回按钮、一个完整头像、MellowBlog 与年级课程两行文字。返回后保持所选学期。
- 下方文件列表独立滚动，选中文件后保持列表位置；深链接刷新时确保选中文件可见。
- 复用 MD_Demo 自带 markdown-it 14.2.0，保留 MIT 许可，关闭原始 HTML。正文参考细则的标题、引用、列表、表格和代码排版，并由 h1–h6 自动生成章节导航。
- 本地目录接口返回 Markdown 文件路径、自然排序文件名和修改版本；每秒检查目录及当前正文的变化，空目录也保留。本轮内容数量仅统计 Markdown，忽略 .gitkeep 和图片附件。
- 在 docs_learning/大一上/课程1 新建测试文档01.md 至测试文档50.md，每篇包含多级标题与排版样例，均明确为测试内容。
- 全站隐藏原生滚动条，保留滚轮、触摸与键盘滚动。窄屏使用限制高度的文件列表和独立正文滚动区域。
- Edge 实测 50 个文件、滚动至第 50 篇、章节跳转、文件深链接刷新、当前文档保存后的自动更新、返回学期、390px 无横向溢出以及关闭系统透明偏好时仍保留玻璃材质。未验证线上部署。
- npm run dev 优先监听 4173；端口占用时自动尝试后续端口，以终端打印地址为准。

## 12. 课程阅读页桌面端视觉修正（2026-09-12）

- 课程阅读页沿用主页的 `minmax(220px, 240px) + 主内容区` 桌面网格，避免从主页进入课程后左栏宽度发生割裂。
- 返回学在南雍使用圆形 SVG 按钮；品牌图标、MellowBlog 与年级课程上下文仍作为同一组顶部识别信息。
- 章节导航与正文使用固定的 `220px + 32px + 正文` 网格，导航块和正文从同一内容起点对齐，不再使用不一致的额外左右距离。
- Markdown 围栏代码由本地 markdown-it 高亮回调处理，针对 JavaScript/TypeScript、Python、Bash、HTML、CSS、JSON 等语言输出安全的语法 token；代码块采用 Obsidian 风格的深色表面与 Cascadia Code/JetBrains Mono 等宽字体回退链。
- 本轮只校正桌面端；滚动区域继续隐藏系统默认滚动条，但保留滚轮、触摸板和键盘滚动能力。
- 课程阅读页左栏不再显示头像，避免返回控件、品牌和课程上下文在窄桌面窗口中拥挤；主页个人信息区的头像保留。
- 章节导航与正文改为左侧栏到章节导航、章节导航到正文均为 48px 的桌面网格；正文最大宽度为 760px，右侧保留更大的阅读余量，不将整个内容组强行居中。

## 13. 学习目录 JSON 化与界面过渡（2026-09-12）

- `docs_learning/learning.json` 作为学习区的展示数据源，使用 `year`、`session`、`course` 保存学期、课程、教师与 Markdown 文件列表；每门课程的 `files` 与实际目录内的 Markdown 文件一一对应。
- 本地预览服务器启动时扫描 `docs_learning` 并校准 `learning.json`；本地运行期间目录接口也会重新校准，因此新增或删除课程、Markdown 文件后无需改动渲染代码。教师名等元数据从 JSON 保留。
- GitHub Pages 直接读取已提交的 `docs_learning/learning.json`，不再依赖 GitHub API 递归扫描目录；发布新课程或文件时同步提交 JSON。
- 学在南雍课程卡片从 JSON 显示老师 SVG 图标、教师字段与内容数量；页面视图切换增加轻量渐入，尊重减少动态效果偏好。
- JSON 中每个学习大类使用 `weight` 控制顶部切换顺序，服务端按权重从小到大排序；权重相同才按名称兜底排序。
- 前端目录解析兼容旧版本地服务器返回的 `subjects` 数组格式，避免旧预览端口未重启时因格式差异显示空白；新版数据仍以 `learning.json` 的 `catalog` 为准。

## 14. 学期切换课程卡片分行渐入计划（2026-09-12）

### 14.1 需求解释与范围

本条需求中的“学在金陵”按当前页面结构解释为“学在南雍”中的学期切换。仅调整点击顶部学期按钮后的课程卡片过渡，不改变左侧站点导航、顶部学期按钮、页面背景、课程卡片内容、目录 JSON 或课程跳转逻辑。

当前问题：

1. 点击学期按钮时，`#content-root` 整体执行渐入，导致标题、切换按钮和课程卡片一起运动。
2. 现有过渡时长约 420ms，体感过快。
3. 课程卡片没有按网格行分组，而是同一时间全部出现，缺少阅读顺序。

目标效果：

1. 学期切换时，标题和顶部切换区保持稳定；只有课程卡片区域执行过渡。
2. 过渡主时长统一为 1000ms。
3. 卡片按视觉网格逐行展开：同一行同时出现，下一行稍后出现，不做逐卡片的随机或左右交错动画。

### 14.2 动效行为规格

- 学期切换的触发点为顶部 `[data-study-term]` 按钮点击或键盘激活。
- 切换按钮本身保留现有 Ripple，不把 Ripple 时长与卡片渐入绑定。
- `.content-view`、页面标题、`.section-tabs` 不添加或重新触发整体渐入。
- `.course-grid` 作为唯一过渡区域；课程卡片先处于轻微下移、低透明度状态，再回到正常位置和透明度。
- 每一行的卡片共享同一个开始时间；行与行之间使用固定延迟，建议为 140ms。
- 单张卡片动画时长为 1000ms，采用无明显回弹的平滑缓动；总时长由行数自然增加，不通过压缩单卡片时长来实现“快闪”。
- 同一学期重复点击当前按钮时不重复刷新课程卡片，也不重新播放动画。
- 在动画播放期间再次切换学期时，立即清理旧卡片的动画状态，并从当前新列表重新计算行号与延迟，避免旧动画残留。

### 14.3 实现计划

1. 将当前通用页面进入动画与学期切换解耦：页面路由切换仍可保留整体渐入，但学期按钮事件不再调用整体内容渐入。
2. 渲染课程卡片后读取每张卡片的实际 `offsetTop`，按相同的顶部位置归并为网格行；不写死三列或行数，以适配桌面端窗口宽度和后续课程数量变化。
3. 为卡片写入行序号或 CSS 自定义属性，例如 `--course-row`，由 CSS 统一计算 `animation-delay`。
4. 给课程卡片增加专用的进入类名和 `@keyframes`，不复用 Ripple 或 Markdown 阅读页的动画类名。
5. 在课程卡片区域替换完成后强制触发一次可控的重排，再添加进入类，确保每次学期切换都能稳定播放且不影响卡片布局。
6. 监听动画结束并清理进入类与临时行号属性，避免长期保留动画状态；动画过程中允许正常点击卡片，不阻塞用户操作。

### 14.4 可访问性与降级

- `prefers-reduced-motion: reduce` 下不播放 1000ms 的位移动画，改为短时透明度变化或直接显示课程卡片。
- 不依赖颜色变化表达卡片是否出现，确保键盘焦点、选中状态与老师/内容数量信息在动画前后保持可见。
- 无 JavaScript 动画能力或动画 API 不可用时，课程卡片正常显示，不影响学期切换和跳转。
- 不新增系统滚动条，不改变现有滚动区域和液态玻璃材质。

### 14.5 验收标准

1. 在“大一上”与“大一下”等有不同课程内容的学期之间切换时，标题和顶部切换区不发生整体淡入或位移。
2. 课程卡片区域的动画计算时长为 1000ms；每一行的延迟相同，后一行延迟大于前一行。
3. 同一行卡片同时出现，不能表现为一张接一张的随机顺序。
4. 连续快速切换学期后，页面不出现旧卡片、重复卡片或动画叠加。
5. 课程卡片仍可点击进入对应课程，Ripple 仍保持 1s 且不被卡片渐入替代。
6. 桌面端检查不同窗口宽度下的行识别；减少动态效果偏好下内容直接可读。

### 14.6 实施与验收记录

- 学期切换保留标题及学期按钮的 DOM、焦点和 Ripple，仅替换课程网格的子节点；重复点击当前学期不重建卡片。
- 根据卡片实际 offsetTop 分行，每行持续 1000ms，行间延迟 140ms；动画结束或取消后清理类名与行号。快速切换会移除旧卡片及其动效。
- 按 Apple Design 的及时响应与减少动态原则，不等待旧动画结束才切换；减少动态偏好下直接显示卡片。
- Edge 1440px 桌面验收通过：标题及按钮节点保持不变，整体动画不触发，5 张卡片延迟为 0/0/0/140/140ms，时长均为 1s；快速切换、重复点击和减少动态偏好检查通过。JavaScript 语法与差异检查通过。

## 15. 课程文件类型、公式与阅读页进入动效（2026-09-12）

### 15.1 交付范围

- 课程文件扫描同时识别 `.md` 与 `.pdf`，并将 `type` 写入 `docs_learning/learning.json`；文件名、路径、类型和版本由目录同步生成。
- 文件列表只显示两个 SVG 圆角工具按钮：筛选与排序。鼠标悬浮、键盘聚焦或点击按钮时，在按钮附近打开液态玻璃小卡片；筛选项为全部/MD/PDF，排序项为名称/类型。
- 每个文件项在左侧显示文件类型标签：MD 使用透明样式，PDF 使用红色样式。PDF 通过浏览器内置 PDF Viewer 的 iframe 展示。
- Markdown 公式由本地 KaTeX 渲染，保护围栏代码、行内代码和公式分隔符；公式先被占位处理，再交给 markdown-it，因此公式内的 `#`、`##` 不会生成错误章节，标题中的公式仍属于同一个章节项。
- 章节导航拆分为固定标题与可滚动列表；标题“章节导航”不随列表滚动，所有滚动区域继续隐藏系统默认滚动条。
- 进入课程阅读页时，左侧文件列表从左向右进入，右侧课程内容渐显，单次动效持续 1000ms；正文文件切换也使用独立的 1000ms 渐入。减少动态效果偏好下关闭这些动效。

### 15.2 测试内容

- 新建 `docs_learning/大一上/公式测试/公式测试.md`，覆盖行内公式、块公式、标题内公式、公式中的井号以及代码块中的美元符号。
- 本地接口启动时重新扫描目录，确保“公式测试”自动出现在“大一上”课程列表；现有用户目录内容继续由同步结果保留。

### 15.3 验收标准

1. 课程页面的 MD 文件项显示透明 MD 标签；未来新增 PDF 后显示红色 PDF 标签并可用内置 Viewer 打开。
2. 悬浮筛选/排序 SVG 图标能打开玻璃小卡片，选择后列表立即更新，列表滚动位置不被无故重置。
3. 公式可见为排版后的数学公式；标题内公式只对应一个章节导航项目，公式内容中的 `#` 不会污染章节导航。
4. 章节导航标题位置固定，章节列表可独立滚动；正文区域没有系统默认滚动条。
5. 进入课程阅读页时左栏与右栏均按 1000ms 动画进入；页面切换和文件切换不出现空白或脚本错误。

## 16. 文件工具浮层稳定性修正（2026-09-12）

- 文件项保留类型标签，删除与类型重复的通用文档 SVG；筛选与排序工具本身继续使用 SVG 圆角按钮。
- 筛选与排序面板改为 HTML 中各自唯一的固定实例，不在鼠标事件中重复插入；两个面板互斥，避免同时打开造成重影和阴影叠加。
- 按钮与对应面板组成连续的悬浮交互区，使用 `pointerenter/pointerleave`、焦点状态和 280ms 关闭延迟；鼠标从按钮移动到面板或在面板内选择时不会意外消失。
- 面板通过 `data-state`、透明度、位移和缩放完成渐显/渐隐，不再用 `hidden` 直接切断动画；筛选或排序后保持当前面板和选项状态。
- `Escape`、点击外部和焦点离开支持轻量关闭；减少动态效果偏好下关闭位移和渐变动画。
