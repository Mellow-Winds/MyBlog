## 一、选择题（请选择正确答案）（选项缺失）

1. Java 中哪些是基本数据类型、哪些是引用数据类型？

---

## 二、`main` 方法

1. `main` 方法一定要用 `psvm`（`public static void main`）修饰吗？

2. `main` 方法的参数名必须是 `args` 吗？

---

## 三、多态

1. 多态只能用方法重写实现吗？

2. 父类引用可以指向子类对象吗？

---

## 四、静态与实例

1. 静态方法可以调用非静态变量吗？

---

## 五、λ 演算（Lambda 演算）

1. 什么是 α 规约（α-conversion）？

2. 什么是 β 规约（β-reduction）？

3. 请解释 `SUCC ONE` 的含义或结果。

---

## 六、看程序写结果（难度：不是很复杂）

请写出以下程序的运行结果。

---

## 七、`String` 内存分析

给定以下代码：

```java
String str1 = "abc";
String str2 = new String("abc");
String str3 = "abc" + str1;
String str4 = "abcabc";
```

1. 几个 `str` 分别存在哪里（堆、栈、常量池等）？

2. 使用 `==` 和 `equals()` 比较时，哪些结果是 `true`，哪些是 `false`？

---

## 八、类图与图书馆系统

请根据以下描述，画出类图并标明关系（需包含类图四要素：类名、属性、方法、关系）：

- 图书馆与书
- 图书与借阅记录
- 读者与借阅记录
- 图书馆与借阅记录
- 读者与图书

---

## 九、银行存钱系统代码设计

1. 用面向对象思想，说说银行账户系统中哪里不合理。

2. 请对不合理之处进行更改。

---

## 十、数据流图描述存款取款过程

请根据以下描述绘制数据流图：

> 储户填写存款单或取款单，由业务员键入系统。如果是存款单，系统记录存款信息（可以有密码或没有密码）并打印存款明细给储户；如果是取款，系统确认密码是否正确或者没有密码，打印取款清单给储户。

---

## 十一、字节码与 JVM

1. JVM 内存区域划分：堆、栈、方法区分别存储什么？

2. `invoke` 的几个指令（如 `invokevirtual`、`invokestatic` 等）分别解释其作用。

3. 给定一段代码，问每个方法对应 `invoke` 的哪个指令。

---

## 十二、操作数栈与局部变量表

给定以下代码：

```java
int a = 1;
int b = 2;
int c = a + b;
```

请解释以下字节码指令在操作数栈与局部变量表中的执行过程：

`iconst`、`istore`、`iload`、`iadd`

---

## 十三、递归

请使用递归生成 n 对括号的所有合法形式。

**要求：** 不允许使用循环体（如 `for`、`while` 等）。