1. 关于检验空和null
检验`null`必须放在前面，再写检验空的情况，否则会报错。

2. 关于String的拼接
字符串不可变，必须使用`StringBuilder`拼接后再使用`s.toString()`转回字符串。

3. 移除字符串非字母数字的方法
`s = s.replaceAll("[^a-zA-Z1-9]", "")`

4. 字符串转小写的方法
`s = s.toLowerCase()`
